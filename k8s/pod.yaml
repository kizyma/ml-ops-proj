apiVersion: v1
kind: Pod
metadata:
  name: pythia-nlp-pod
  labels:
    purpose: demonstrate-pythia-nlp
spec:
  containers:
  - name: pythia-nlp
    image: ghcr.io/kizyma/ml-ops-proj:latest 
    ports:
    - containerPort: 8000
